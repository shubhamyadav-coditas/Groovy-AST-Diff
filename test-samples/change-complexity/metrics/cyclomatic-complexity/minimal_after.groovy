// Test: Minimal complexity - after (added if statement)
// Expected CC: 2 (base + 1 if)

class Simple {
    void run(boolean flag) {
        if (flag) {
            println "Hello World"
        }
    }
}
